// Shared constants for scheduler components.
// Scheduler operates on a Monday-Friday work week (no Saturday).

export const DAYS = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday']

export const DAYS_COUNT = DAYS.length
