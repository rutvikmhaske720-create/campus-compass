export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your CampusCompass account to manage timetables and scheduling.',
}

export default function SignInLayout({ children }) {
  return children
}
