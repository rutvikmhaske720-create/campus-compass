export const metadata = {
  title: 'Faculty',
  description: 'Manage faculty members, their workloads, and availability for department scheduling.',
}

export default function FacultyLayout({ children }) {
  return children
}
