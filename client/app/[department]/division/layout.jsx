export const metadata = {
  title: 'Divisions',
  description: 'Manage divisions, batches, and student groupings for timetable scheduling.',
}

export default function DivisionLayout({ children }) {
  return children
}
