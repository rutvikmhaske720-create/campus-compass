export const metadata = {
  title: 'Department Notifications',
  description: 'Send and review notifications within the department.',
}

export default function DepartmentNotifyLayout({ children }) {
  return children
}
