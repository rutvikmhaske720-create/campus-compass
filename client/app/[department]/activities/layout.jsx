export const metadata = {
  title: 'Activities',
  description: 'Define subjects, labs, and academic activities for department timetable generation.',
}

export default function ActivitiesLayout({ children }) {
  return children
}
