export const metadata = {
  title: 'Rooms',
  description: 'Configure classrooms, labs, and shared spaces for timetable generation.',
}

export default function RoomsLayout({ children }) {
  return children
}
