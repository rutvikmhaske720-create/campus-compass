export const metadata = {
  title: 'MDM Configuration',
  description: 'Configure Multi-Disciplinary Module (MDM) slots and constraints for NEP-aligned scheduling.',
}

export default function MDMConfigLayout({ children }) {
  return children
}
