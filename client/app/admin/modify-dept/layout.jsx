export const metadata = {
  title: 'Modify Departments',
  description: 'Add, edit, or remove department configurations for university timetable generation.',
}

export default function ModifyDeptLayout({ children }) {
  return children
}
