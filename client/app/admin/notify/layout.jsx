export const metadata = {
  title: 'Notifications',
  description: 'Send notifications and announcements to departments and faculty.',
}

export default function AdminNotifyLayout({ children }) {
  return children
}
