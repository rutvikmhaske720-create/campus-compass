export const metadata = {
  title: 'Admin Dashboard',
  description: 'Administrative dashboard for managing timetables, departments, and scheduling across the university.',
}

export default function AdminDashboardLayout({ children }) {
  return children
}
